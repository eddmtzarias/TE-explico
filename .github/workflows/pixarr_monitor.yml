name: PixARR Design Monitor

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  schedule:
    # Run daily at 00:00 UTC
    - cron: '0 0 * * *'

jobs:
  integrity-check:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v3
      
      - name: 🐍 Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      
      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          # Add any required dependencies here
      
      - name: 🔧 Auto-Fix Integrity Issues
        id: autofix
        continue-on-error: true
        run: |
          python scripts/fix_integrity_anomalies.py
      
      - name: 🔍 Run Integrity Audit
        id: audit
        run: |
          python -c "
          from pixarr_design.core.agent import PixARRAgent
          from pixarr_design.config.settings import Settings
          import sys
          
          Settings.ensure_directories()
          agent = PixARRAgent()
          agent.activate()
          
          results = agent.audit_integrity()
          anomalies = results['summary']['anomalies']
          
          print(f'Files verified: {results[\"summary\"][\"total_files\"]}')
          print(f'Anomalies detected: {anomalies}')
          
          if anomalies > 0:
              print('⚠️ ANOMALIES DETECTED!')
              sys.exit(1)
          else:
              print('✅ All files passed integrity audit')
          "
      
      - name: 📊 Upload audit results
        if: always()
        uses: actions/upload-artifact@v3
        with:
          name: integrity-audit-results
          path: .pixarr_metadata/
