# OmniMaestro - Dependencias Python
# Instalación: pip install -r requirements.txt

# === Core Dependencies (Vercel-compatible) ===
openai>=1.7.0
requests>=2.31.0
python-dotenv>=1.0.0

# === Desktop ONLY (excluded in Vercel via vercel.json) ===
# Estas se instalarán localmente pero NO se incluirán en el bundle de Vercel
beautifulsoup4>=4.12.0
pillow>=10.0.0
# anthropic>=0.8.0

# === PixARR Design System ===
# (Dependencias del sistema de integridad de archivos)

# === OCR (Desktop only - excluded in Vercel) ===
# pytesseract>=0.3.10
# easyocr>=1.7.0
# opencv-python-headless>=4.8.0

# === Vision / Screenshots (Desktop only) ===
# pyautogui>=0.9.54
# mss>=9.0.0

# === Audio (para funcionalidad de voz) ===
# pyttsx3>=2.90
# SpeechRecognition>=3.10.0

# === Testing (Development only - not needed in Vercel) ===
# pytest>=7.4.0
# pytest-cov>=4.1.0

# === Development (Not needed in Vercel) ===
# black>=23.0.0
# flake8>=6.0.0
# mypy>=1.0.0
